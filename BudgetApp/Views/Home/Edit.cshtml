@model BudgetApp.Domain.Concrete.LedgerEntry

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm("Edit", "Home", new { add = ViewBag.Add}, FormMethod.Post))
{
    @Html.EditorForModel()

    <input type="submit" value="Save"  />
}
